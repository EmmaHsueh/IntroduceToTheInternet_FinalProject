import React, { useState } from 'react';export default function Comments(){ const [list,setList]=useState([{id:1,name:'Alice',text:'這是一則範例留言。'},{id:2,name:'Bob',text:'第二則留言示意。'}]); const [text,setText]=useState(''); function add(){ if(!text) return; setList(prev=> [{id:Date.now(),name:'你',text},...prev]); setText(''); } return (<div><div style={{marginBottom:12}}><textarea value={text} onChange={e=>setText(e.target.value)} rows={3} style={{width:'100%'}}></textarea><button onClick={add} style={{marginTop:8}}>新增留言</button></div><div>{list.map(c=>(<div key={c.id} style={{padding:10,borderBottom:'1px solid #eee'}}><strong>{c.name}</strong><div>{c.text}</div></div>))}</div></div>); }